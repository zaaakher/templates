variables:
  main_domain: ${domain}
  api_domain: ${domain}

config:
  domains:
    - serviceName: kibana
      port: 5601
      host: ${main_domain}
    - serviceName: elasticsearch
      port: 9200
      host: ${api_domain}

  env: []

  mounts: [] 