variables:
  main_domain: ${randomDomain}
  admin_password: ${generatePassword(32)}
  mysql_password: ${generatePassword(32)}
  mysql_root_password: ${generatePassword(32)}
  app_secret: ${generatePassword(32)}

config:
  domains:
    - serviceName: app
      port: 8001
      host: ${main_domain}

  env:
    KI_HOST: ${main_domain}
    KI_ADMINMAIL: "admin@kimai.local"
    KI_ADMINPASS: ${admin_password}
    KI_MYSQL_ROOT_PASSWORD: ${mysql_root_password}
    KI_MYSQL_PASSWORD: ${mysql_password}
    KI_APP_SECRET: ${app_secret}

  mounts: [] 