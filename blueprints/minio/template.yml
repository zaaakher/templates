variables:
  main_domain: ${domain}
  api_domain: ${domain}

config:
  domains:
    - serviceName: minio
      port: 9001
      host: ${main_domain}
    - serviceName: minio
      port: 9000
      host: ${api_domain}

  env: {}

  mounts: [] 