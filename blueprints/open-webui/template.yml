variables:
  main_domain: ${randomDomain}

config:
  domains:
    - serviceName: open-webui
      port: 8080
      host: ${main_domain}

  env:
    OLLAMA_DOCKER_TAG: "0.1.47"
    WEBUI_DOCKER_TAG: "0.3.7"

  mounts: [] 