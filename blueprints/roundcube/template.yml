variables:
  main_domain: ${domain}

config:
  domains:
    - serviceName: roundcubemail
      port: 80
      host: ${main_domain}

  env:
    DEFAULT_HOST: "tls://mail.example.com"
    SMTP_SERVER: "tls://mail.example.com"

  mounts: [] 