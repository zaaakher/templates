variables:
  main_domain: ${domain}
  secret_key: ${password}

config:
  domains:
    - serviceName: server
      port: 8080
      host: ${main_domain}

  env:
    SD_USERNAME: "admin"
    SD_PASSWORD: ${secret_key}

  mounts: [] 