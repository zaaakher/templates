variables:
  main_domain: ${randomDomain}

config:
  domains:
    - serviceName: wiki
      port: 3000
      host: ${main_domain}

  env:
    # Database Setup
    POSTGRES_USER: "wikijs"
    POSTGRES_PASSWORD: "wikijsrocks"
    POSTGRES_DB: "wiki"
    # WikiJS Database Connection
    DB_TYPE: "postgres"
    DB_HOST: "db"
    DB_PORT: "5432"
    DB_USER: "wikijs"
    DB_PASS: "wikijsrocks"
    DB_NAME: "wiki"

  mounts: [] 