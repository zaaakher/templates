variables:
  main_domain: ${randomDomain}
  metrics_domain: ${randomDomain}

config:
  domains:
    - serviceName: soketi
      port: 6001
      host: ${main_domain}
    - serviceName: soketi
      port: 9601
      host: ${metrics_domain}

  env: {}

  mounts: [] 